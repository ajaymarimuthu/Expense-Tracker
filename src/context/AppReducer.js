export default (state, action) =>{
    switch(action.type){
        default:
            return state;
    }
}


//  App reducer is responsible for managing  all the functionalities (like changing and updating the state) in the Globalstate  